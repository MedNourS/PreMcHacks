import type { Response } from 'express';
import type { loginCredentials } from '../types/authTypes';

export function signUpControl(res: Response, credentials: loginCredentials) {

}

export function loginControl(res: Response, credentials: loginCredentials) {

}